# packages

```bash
apt-get install update && apt-get inastall disk-upgrade

```

## git
Общий рабочий инструмент для любого разработчика

## gear 
Инструмент создания сборочного chroot и установки туда пакетов

## hasher
Инструмент создания сборочного chroot и установки туда пакетов

## hasher-priv
Вспомогательный демон, который запускает сборочные программы

## hasher-rich-chroot

## hasher-rich-chroot-user-utils

## rpmdevtools

Пакет с набором программ для сборки пакетов

```bash
apt-get install rpmdevtools
```

## rpm-utils

## rpm-build
Этот пакет содержит скрипты и исполняемые программы, используемые для создания (сборки) RPM-пакетов.

## rpm-build-licenses
набор макросов RPM, которые выполняют подстановку корректного названия той или иной лицензии. Полезно для унификации и исключения ошибок.

## rpm-macros-cmake
набор макросов для запуска сборки с помощью cmake

## rpm-macros-make
набор макросов для запуска сборки с помощью make

## rpm-macros-generic-compat

## apt-repo 
утилита управления источниками установки пакетов - файлами sources.list

## apt-repo-tools

## vim-plugin-spec_alt-ftplugin
плагин Vim для обеспечения подсветки специфичных для ALT конструкций RPM specfile.

## kernel-build-tools
утилиты для работы со spec файлами для сборки ядра ALT.

## describe-specfile
зависимость для kernel-build-tools


## gcc
Используется для компиляции программ на языке C.

Устанавливаем компилтор для C
```bash
apt-get install gcc 
```

## gcc-c++
Устанавливаем компилтор для C++
```bash
apt-get install gcc-c++
``` 

## make

## cmake
Устанавливаем cmake
```bash
apt-get install cmake
``` 

## cmake gdb

Устанавливаем отладчик Cmake
```bash
apt-get install cmake gdb
```

## rpm-devel 

## rpmlint 

## python 

## bash 

## coreutils 

## diffutils 

## patch 

## rpmdevtools


# VS Code

*   C/C++ Extension Pack
*   CMake
